{
  "build": {
    "image": "python:3.12",
    "command": "pip install -r backend/requirements.txt"
  },
  "start": {
    "command": "gunicorn backend.wsgi:application --chdir backend --bind 0.0.0.0:$PORT"
  }
}
